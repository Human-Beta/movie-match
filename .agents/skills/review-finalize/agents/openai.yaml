interface:
  display_name: "Review Finalize"
  short_description: "Audit docs, tests, commits, and PR gates"
  default_prompt: "Use $review-finalize to audit code-documentation alignment and test coverage, then plan the final conventions, logical commit history, and merge-readiness checks for the current pull request."

policy:
  allow_implicit_invocation: false
