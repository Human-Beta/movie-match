interface:
  display_name: "Review Notes"
  short_description: "Fix, push, and log private PR notes"
  default_prompt: "Use $review-notes to process my private pending review, scan for similar issues, apply verified fixes, push them, and update the PR review log."

policy:
  allow_implicit_invocation: false
